---
$schema: https://holocron.so/frontmatter.json
title: Native React on the GPU
sidebarTitle: Home
description: Build native GPU-accelerated desktop apps with React and TypeScript. No Electron, no web views.
prompt: |
  Write the homepage from @/README.md and @/website/src/components/hero-section.tsx.
  Keep the README import as the body. Hero tagline is native React on the GPU.
---

import { HeroSection } from './components/hero-section.tsx'
import Readme from '../../README.md'

<Above>

<HeroSection />

</Above>

<Readme />
